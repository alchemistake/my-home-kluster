apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-server
spec:
  template:
    spec:
      volumes:
        - name: storage-volume
          hostPath:
            path: /k3s/prometheus/server
          persistentVolumeClaim:
            $patch: delete